name: Test

on:
    push:
        branches:
            - 'main'
        tags:
            - '*'
    pull_request:
        branches:
            - 'main'
    workflow_dispatch:
        inputs:
            matrix:
                description: 'Shopware version matrix'
                type: choice
                options:
                    - reduced
                    - full
                default: reduced

jobs:
    unit-tests:
        runs-on: ubuntu-latest
        name: Unit Tests

        steps:
            - uses: actions/checkout@v6
            - name: Setup PHP
              uses: shivammathur/setup-php@v2
              with:
                  php-version: '8.2'
                  coverage: none
            - name: Install dependencies
              run: |
                  composer install -n --no-dev
                  composer require --dev phpunit/phpunit:^11.0 -n
            - name: Unit Tests
              run: bash bin/phpunit.sh

    resolve-matrix:
        runs-on: ubuntu-latest
        outputs:
            shopware-version: ${{ steps.matrix.outputs.versions }}
            fail-fast: ${{ steps.matrix.outputs.fail-fast }}
        steps:
            - id: matrix
              run: |
                  FULL_VERSIONS='[
                    "v6.4.5","v6.4.6","v6.4.7","v6.4.8","v6.4.9","v6.4.10",
                    "v6.4.11","v6.4.12","v6.4.13","v6.4.14","v6.4.15","v6.4.16",
                    "v6.4.17","v6.4.18","v6.4.19","v6.4.20",
                    "v6.5.0","v6.5.1","v6.5.2","v6.5.3","v6.5.4","v6.5.5",
                    "v6.5.6","v6.5.7","v6.5.8",
                    "v6.6.0","v6.6.1","v6.6.2","v6.6.3","v6.6.4","v6.6.5",
                    "v6.6.6","v6.6.7","v6.6.8","v6.6.9",
                    "v6.7.0","v6.7.1","v6.7.2","v6.7.3","v6.7.4","v6.7.5","v6.7.6",
                    "v6.7.7","v6.7.8","v6.7.9","v6.7.10","v6.7.11","v6.7.12"
                  ]'
                  REDUCED_VERSIONS='["v6.4.20","v6.5.8","v6.6.9","v6.7.12"]'

                  if [ "${{ github.event_name }}" = "push" ] && [[ "${{ github.ref }}" == refs/tags/* ]]; then
                    VERSIONS="$FULL_VERSIONS"
                    FAIL_FAST=false
                  elif [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ "${{ inputs.matrix }}" = "full" ]; then
                    VERSIONS="$FULL_VERSIONS"
                    FAIL_FAST=false
                  else
                    VERSIONS="$REDUCED_VERSIONS"
                    FAIL_FAST=true
                  fi

                  echo "versions=$(echo "$VERSIONS" | jq -c .)" >> "$GITHUB_OUTPUT"
                  echo "fail-fast=$FAIL_FAST" >> "$GITHUB_OUTPUT"

    ci-current:
        needs: resolve-matrix
        runs-on: ubuntu-latest
        strategy:
            max-parallel: 20
            fail-fast: ${{ needs.resolve-matrix.outputs.fail-fast == 'true' }}
            matrix:
                shopware-version: ${{ fromJson(needs.resolve-matrix.outputs.shopware-version) }}
        name: Shopware ${{ matrix.shopware-version }}

        container:
            image: ghcr.io/friendsofshopware/platform-plugin-dev:${{ matrix.shopware-version }}-debian
        env:
            PLUGIN_DIR: /plugins/EasyCreditRatenkauf
            SW_DIR: /opt/shopware
            SW_VERSION: ${{ matrix.shopware-version }}
            EASYCREDIT_PAYMENT_API: true

        steps:
            - uses: actions/checkout@v6
            - name: Debug
              run: |
                  php -v
                  php -m
                  composer -V
                  env
                  node -v
            - name: Initialize cache
              uses: actions/cache@v5
              with:
                path: |
                  ~/.cache/yarn
                  ~/.config/yarn
                  ~/.cache/ms-playwright
                  ~/.cache/composer
                  ~/.npm
                  node_modules
                key: yarn-cache-${{ runner.os }}-${{ hashFiles('**/yarn.lock') }}
                restore-keys: |
                  yarn-cache-${{ runner.os }}-
            - name: Start mysql server
              run: start-mysql
            - name: Link plugin with shopware installation and install composer deps
              run: |
                  ln -s $GITHUB_WORKSPACE $PLUGIN_DIR
                  cd $PLUGIN_DIR
                  composer remove shopware/core frosh/shopware-rector
                  composer install -n --no-dev
            - name: Prepare Shopware
              run: |
                  cd $SW_DIR
                  ./bin/console sales-channel:create:storefront --url http://localhost

                  if echo "${{ matrix.shopware-version }}" | grep -q '^v6\.[567].\.*'; then
                    composer run init:js
                    composer run build:js
                    composer run build:js:storefront
                  fi

                  ./bin/console theme:change --all -n Storefront
                  php bin/console dal:refresh:index -qn
                  php bin/console user:create --admin --email=john@doe.com --firstName="John" --lastName="Doe" --password=shopware --no-interaction admin

                  echo 'APP_ENV="prod"' >> .env
            - name: Static Analyze
              run: |
                  cd $PLUGIN_DIR
                  php bin/static-analyze.sh
            - name: Coding Style
              if: false
              run: |
                  cd $PLUGIN_DIR
                  php vendor/bin/php-cs-fixer check ./src
            - name: Install & activate Plugin
              run: |
                  cd $SW_DIR
                  php bin/console plugin:refresh
                  php bin/console plugin:install EasyCreditRatenkauf
                  php bin/console plugin:activate EasyCreditRatenkauf
                  php bin/console system:config:set EasyCreditRatenkauf.config.webshopId ${{ secrets.EASYCREDITAPIKEY }}
                  php bin/console system:config:set EasyCreditRatenkauf.config.apiPassword ${{ secrets.EASYCREDITAPIPASSWORD }}
                  php bin/console system:config:set EasyCreditRatenkauf.config.apiSignature ${{ secrets.EASYCREDITAPISIGNATURE }}
                  php bin/console system:config:set EasyCreditRatenkauf.config.debug 1
                  php bin/console system:config:set core.basicInformation.useDefaultCookieConsent 0

                  # only productNumber should be searchable for e2e tests
                  mysql -u root -proot shopware -e "update product_search_config_field set searchable = 0 WHERE field != 'productNumber';"

                  php bin/console cache:clear
            - name: Install Dependencies
              run: |
                  npm install --global yarn
                  yarn install --frozen-lockfile
                  curl -sS https://get.symfony.com/cli/installer | bash
            - name: Setup Playwright
              run: |
                  # platform-plugin-dev v6.4.* and v6.5.* are Debian Bullseye. After
                  # Debian 11 LTS, apt --with-deps fails on expired/404 debian-security;
                  # browser deps are already on the image, so install Chromium only.
                  if echo "${{ matrix.shopware-version }}" | grep -q '^v6\.[45]\.'; then
                    yarn playwright install chromium
                  else
                    yarn playwright install --with-deps chromium
                  fi
            - name: Playwright E2E Tests
              run: |
                  cd $PLUGIN_DIR
                  VERSION=${{ matrix.shopware-version }} yarn playwright test -c tests/
            - name: Save Shopware Logs for Debugging
              if: always()
              run: |
                  LOG_DIR=$PLUGIN_DIR/playwright-report/logs
                  mkdir -p $LOG_DIR
                  cp -r $SW_DIR/var/log/* $LOG_DIR/
                  for i in $LOG_DIR/*; do
                    f=$(basename $i)
                    echo "<a href=\"$(basename $f)\">$f</a><br />" >> $LOG_DIR/index.html
                  done;
            - name: Upload Playwright report
              if: always()
              uses: actions/upload-artifact@v4
              with:
                  name: playwright-report-${{ matrix.shopware-version }}
                  path: ${{ env.PLUGIN_DIR }}/playwright-report/
                  if-no-files-found: warn
            - name: Deactivate & uninstall Plugin
              if: always()
              run: |
                  cd $SW_DIR
                  php bin/console plugin:deactivate EasyCreditRatenkauf
                  php bin/console plugin:uninstall EasyCreditRatenkauf
                  php bin/console cache:clear

    deploy-report:
        name: Deploy Playwright reports
        needs: [resolve-matrix, ci-current]
        if: ${{ always() && needs.ci-current.result != 'cancelled' }}
        runs-on: ubuntu-latest
        steps:
            - name: Download all Playwright reports
              id: download-reports
              continue-on-error: true
              uses: actions/download-artifact@v4
              with:
                  pattern: playwright-report-*
                  path: artifacts
            - name: Build combined report site
              id: build-site
              run: |
                  shopt -s nullglob
                  mkdir -p site

                  if [ ! -d artifacts ] || [ -z "$(ls -A artifacts 2>/dev/null)" ]; then
                    echo "No Playwright reports found to deploy."
                    exit 0
                  fi

                  for report_dir in artifacts/playwright-report-*; do
                    [ -d "$report_dir" ] || continue
                    version="${report_dir#artifacts/playwright-report-}"
                    cp -R "$report_dir" "site/$version"
                  done

                  if [ ! "$(ls -A site 2>/dev/null)" ]; then
                    echo "No Playwright reports found to deploy."
                    exit 0
                  fi

                  echo "has_reports=true" >> "$GITHUB_OUTPUT"

                  {
                    echo '<!DOCTYPE html>'
                    echo '<html lang="en"><head><meta charset="utf-8">'
                    echo '<title>easyCredit Playwright Reports</title>'
                    echo '<style>body{font-family:sans-serif;max-width:48rem;margin:2rem auto;padding:0 1rem}'
                    echo 'ul{line-height:1.8}</style></head><body>'
                    echo '<h1>easyCredit Playwright Reports</h1><ul>'
                    for version_dir in site/*/; do
                      version="$(basename "$version_dir")"
                      echo "<li><a href=\"$version/\">Shopware $version</a></li>"
                    done
                    echo '</ul></body></html>'
                  } > site/index.html
            - name: Deploy Report
              if: steps.build-site.outputs.has_reports == 'true'
              uses: cloudflare/wrangler-action@v4
              with:
                  apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
                  accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
                  command: pages deploy site/ --project-name=easycredit-playwright-reports
